// Central SEO configuration.
// IMPORTANT: SITE_URL is a placeholder until the site has a live production
// domain. Update it here (one place) once nileorphancare.org (or whatever
// domain is chosen) is live — this value feeds canonical tags, Open Graph
// URLs, robots.txt, and sitemap.xml everywhere in the app.
export const SITE_URL = 'https://www.nileorphancare.org';
export const SITE_NAME = 'Nile Orphan Care';
export const DEFAULT_OG_IMAGE = `${SITE_URL}/images/noc_logo.png`;
export const TWITTER_HANDLE = null; // set if/when NOC has a confirmed X/Twitter handle
